name: ziwei_core
description: A pure Dart, configuration-driven Ziwei Doushu (Purple Star Astrology) engine supporting a 6000-year timeline with high-precision ephemeris and JSON patching.
version: 0.9.2
repository: https://github.com/RedSC1/ziwei_core

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  tuple: ^2.0.2
  sxwnl_spa_dart: ^0.10.0
  bazi_core: ^0.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
